import { db } from "@/lib/db";
import { qcItemDict } from "@/lib/db/schema";
import { type ActionRow, type QcItemRow } from "../../../lib/db/types";
export async function getQcItemRows(
  actions: ActionRow[]
): Promise<QcItemRow[]> {

  
}
